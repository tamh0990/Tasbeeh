workflows:
  ios-workflow:
    name: iOS Workflow
    environment:
      xcode: latest
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Build project
        script: |
          npm run build
    artifacts:
      - build/**
